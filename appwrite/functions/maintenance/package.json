{
  "name": "maintenance",
  "version": "1.0.0",
  "private": true,
  "description": "Appwrite Function: two administrative triggers - the caller's own account deletion cascade (HTTP-invoked) and syncing a list's task permissions to its current membership (database event) - merged into one function to fit the Appwrite Cloud free-tier limit of 2 functions per project. Ported from functions/src/accountDeletion.ts's deleteAccount, plus the new syncListPermissions (see its own file comment).",
  "engines": {
    "node": ">=22"
  },
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "node-appwrite": "^28.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.9.3"
  }
}
