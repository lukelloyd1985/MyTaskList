{
  "name": "sync-list-permissions",
  "version": "1.0.0",
  "private": true,
  "description": "Appwrite Function: fans a list's ownerId/memberIds out to per-document permissions on every task under that list, since Appwrite has no live rule evaluation against a parent document.",
  "engines": {
    "node": ">=22"
  },
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "node-appwrite": "^28.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.9.3"
  }
}
