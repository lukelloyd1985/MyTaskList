{
  "name": "notifications",
  "version": "1.0.0",
  "private": true,
  "description": "Appwrite Function: sends FCM push notifications for two triggers - a task's assignee changing (database event) and the due-date reminder sweep (CRON) - merged into one function to fit the Appwrite Cloud free-tier limit of 2 functions per project. Ported from functions/src/notifications.ts's onTaskWrite and dueDateReminders.",
  "engines": {
    "node": ">=22"
  },
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "node-appwrite": "^28.0.0",
    "google-auth-library": "^11.0.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.9.3"
  }
}
